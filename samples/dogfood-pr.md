<!--
  Generated by DevFlow v0.1.0
  kind: pr
  audience: reviewer
  repo: Auto-Company
  note: polished dogfood PR narrative for Cycle 3
  created: 2026-07-23T08:15:00.000Z
-->

## PR title suggestion
feat(devflow): ship BYOK handoff CLI, landing, and kill metrics (Cycle 3)

## Summary
Adds `projects/devflow` — a constrained MVP that emits **durable handoff / PR markdown** from git-aware context. Implements Cycle 2 CONDITIONAL GO scope: artifact CLI, not interactive codebase chat. Includes static founding-Pro landing ($9) with WTP instrumentation and a 10-seat beta ledger.

## Motivation
Interactive “explain this repo” is free inside Cursor and peers. Paid residue is **portable handoff packets** for agencies and multi-tool teams. We need a shippable wedge with 2-week kill criteria, not another chat SKU.

## What changed
- New package: TypeScript CLI (`handoff` / `explain` / `pr`) + offline fallback
- Git context collector with token/COGS-oriented caps
- Anthropic BYOK integration (Haiku default)
- `metrics/events.jsonl` + `metrics/beta-seats.json` + kill dashboard stub
- `landing/` static site (waitlist + WTP $0/$9/$19/$29)
- Cycle 3 docs under `docs/{fullstack,product,marketing,devops,qa,operations}/`

## Risk & review focus
- Confirm **no secrets** in samples or metrics
- Confirm product copy never positions as chat-first
- Context packing: truncation markers present; no `node_modules` / lockfile slurping
- `bin/devflow.js` requires `npm run build` (dist) — acceptable for MVP

## Test plan
- [x] `npm install && npm run build` in `projects/devflow`
- [x] Offline handoff + pr smoke
- [x] Metrics lines appended
- [ ] Optional: live BYOK run when key available
- [ ] Manual landing form submit + export helper

## Rollout / follow-ups
- [ ] Deploy landing (Cloudflare Pages) when `CLOUDFLARE_API_TOKEN` present
- [ ] Outreach for remaining 9 beta seats
- [ ] Re-run kill dashboard in 7 and 14 days
- [ ] If kill fires → AgentWatch Cycle 2 battery (no sunk cost)

---
*DevFlow PR narrative sample.*
