# DevFlow landing

Public static landing for **DevFlow** — git-native handoff & shipping-artifact CLI.

Source monorepo path: `projects/devflow/landing` in Auto-Company.

## Local

Open `index.html` or any static server on this directory.

## Product

One CLI command → portable walkthrough your next human can trust when Cursor is closed.
